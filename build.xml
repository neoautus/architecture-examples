<?xml version="1.0"?>
<project name="architecture-examples" default="build-all">

    <import file="configure.xml"/>

    <target name="build-stage">
        <subant antfile="build-stage.xml" buildpath="${basedir}/00-apache-karaf"/>
    </target>

    <target name="build-all">
        <iterate target=""/>
    </target>

    <target name="clean">
        <iterate target="clean"/>
    </target>

</project>
